let status = require("./status");
const { toggleStatus } = require("./toggleStatus");

status = toggleStatus(status);
status = toggleStatus(status);
status = toggleStatus(status);
status = toggleStatus(status);
status = toggleStatus(status);

console.log(status);
